export default function validarConfig(valores) {
  let errores = {};

  // if (!valores.api) {
  //   errores.api = "La sigla es obligatorio";
  // }

  // if (!valores.nombre) {
  //   errores.nombre = "El Nombre es obligatorio";
  // }

  return errores;
}
